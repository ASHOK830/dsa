#169. Majority Element

class Solution:
    def majorityElement(self, nums: List[int]) -> int:
      


            maxcount = 0
            temp=set(nums)
            for i in temp:
                count = nums.count(i)

                if count > maxcount:
                    maxcount = count
                    n = i
            return n

           
