#48. Rotate Image

        m=len(matrix[0])
        n=len(matrix)

        for i in range(m):
            for j in range(i+1,n):
                matrix[i][j],matrix[j][i]=matrix[j][i],matrix[i][j]
        for row in matrix:
            row.reverse()
        return matrix
#test case
matrix = [
 [1,2,3],
 [4,5,6],
 [7,8,9]
]

OUTPUT:-
[
 [7,4,1],
 [8,5,2],
 [9,6,3]
]
