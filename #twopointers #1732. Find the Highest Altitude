#twopointers
#1732. Find the Highest Altitude
class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        li=[0]
        ans=0
        for i in range(len(gain)):
            ans+=gain[i]
            li.append(ans)
            
        return max(li)
