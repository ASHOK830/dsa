class Solution:
    def finalValueAfterOperations(self, operations: List[str]) -> int:
      x=0
       
      for i in operations:
        if i=="--x" or "x--":
           x=x-1
        if i=="++x"or "x++":
           x=x+1
      print(x)
      return x  
