#two pointers
#moving zeros to the end (move in numbers to front)

class Solution:
    def moveZeroes(self, nums):
  
        j=0
        for i in range(len(nums)):
            if nums[i]!=0:
                nums[i], nums[j]= nums[j], nums[i]
                j=j+1
